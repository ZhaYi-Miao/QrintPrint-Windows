<Window x:Class="QrintPrint.Views.DevicePickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择打印机"
        Height="640" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource Layer01Brush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource SubtitleText}"
                   Text="选择打印机" />
        <TextBlock Grid.Row="1" Margin="0,4,0,16"
                   Style="{StaticResource CaptionText}"
                   Text="支持蓝牙和 USB 连接方式" />

        <Border Grid.Row="2"
                BorderBrush="{DynamicResource ControlStrokeBrush}"
                BorderThickness="1"
                CornerRadius="6">
            <Grid>
                <ListBox x:Name="DeviceList"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="2"
                         SelectionChanged="DeviceList_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="6,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource BodyStrongText}" />
                                    <TextBlock Text="{Binding Subtitle}" Style="{StaticResource CaptionText}" Margin="0,2,0,0" />
                                </StackPanel>
                                <Border Grid.Column="1" Margin="8,0,0,0"
                                        VerticalAlignment="Center"
                                        BorderBrush="{DynamicResource ControlStrokeBrush}"
                                        BorderThickness="1" CornerRadius="4"
                                        Padding="6,2">
                                    <TextBlock Text="{Binding TransportLabel}"
                                               Style="{StaticResource CaptionText}"
                                               Foreground="{DynamicResource TextTertiaryBrush}" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <TextBlock x:Name="EmptyHint" Visibility="Collapsed"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Style="{StaticResource CaptionText}"
                           TextWrapping="Wrap" Margin="20"
                           Text="未发现打印机设备" />
            </Grid>
        </Border>

        <TextBlock x:Name="StatusHint" Grid.Row="3" Margin="0,12,0,0"
                   Style="{StaticResource CaptionText}"
                   Foreground="{DynamicResource TextTertiaryBrush}"
                   TextWrapping="Wrap"
                   Visibility="Collapsed" />

        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Left" Margin="0,16,0,0">
            <Button x:Name="ScanBtn" Padding="12,8" Margin="0,0,8,0"
                    Click="ScanBtn_Click" Content="重新扫描" />
            <Button x:Name="ShowAllBtn" Padding="12,8"
                    Click="ShowAllBtn_Click" Content="显示所有打印机" />
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="ShowLogsBtn" Padding="12,8" Margin="0,0,8,0"
                    Click="ShowLogsBtn_Click" Content="查看日志" />
            <Button x:Name="TestPrintBtn" Padding="12,8" Margin="0,0,8,0"
                    IsEnabled="False"
                    Click="TestPrintBtn_Click" Content="测试打印" />
            <Button x:Name="ConnectBtn" Padding="12,8" Margin="0,0,8,0"
                    IsEnabled="False"
                    Click="ConnectBtn_Click" Content="连接" />
            <Button Padding="12,8" IsCancel="True" Content="取消" />
        </StackPanel>
    </Grid>
</Window>
